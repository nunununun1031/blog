import React from "react";
import { Typography } from "@material-ui/core";
import styles from "./Browsing.module.scss";

const Browsing = () => {
  return (
    <div className={styles.root}>
      <Typography variant="h2">Browsing</Typography>
    </div>
  );
};

export default Browsing;
